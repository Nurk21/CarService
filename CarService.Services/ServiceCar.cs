using CarService.Core.Entities.VehicleType;
using System;
using System.Collections.Generic;
using System.Text;

namespace CarService.Services
{
   /* public class ServiceCar : IBaseService<Car>
    {
        public int EstimatePrice(Car car)
        {
            int price = 0;
            if ((car.Body.BodyCondition >= 0 && car.Body.BodyCondition <= 100) && (car.Brakes.BrakesCondition >= 0 && car.Brakes.BrakesCondition <= 100) && (car.Undercarriage.UndercarriageCondition >= 0 && car.Undercarriage.UndercarriageCondition <= 100))
            {
                price += (100 - car.Body.BodyCondition) / 100 * car.Body.BodyCondition;
            }
        }

        public void Repair()
        {

        }
    }*/
}
